// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    custom1: {
      // main: '#479761',
      main:"#008000", 
    },
  },
});

export default theme;
